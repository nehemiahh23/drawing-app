{
  "name": "server",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx tsc && node dist/app.js",
    "dev": "tsc-watch -b ./tsconfig.json --onSuccess \"nodemon dist/app.js\"",
    "clean": "rm -rf dist && mkdir dist && npx tsc"
  },
  "nodemonConfig": {
    "ignore": [
      "uploads/"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "devDependencies": {
	"nodemon": "^3.1.11",
	"tsc-watch": "^7.2.0",
	"@types/multer": "^2.0.0",
	"@types/express": "^5.0.6",
	"@types/node": "^25.0.9"
},
"dependencies": {
    "cloudinary": "^2.9.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mongoose": "^9.2.0",
    "multer": "^2.0.2"
  }
}
